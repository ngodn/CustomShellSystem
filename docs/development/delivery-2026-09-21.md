# Playable CSS and Eve checkpoint

The user requested a usable delivery before the remaining weekly quota runs
out. The package preserves the tested game baseline while the unfinished
animation work is checkpointed separately.

Deliverables in `dist/delivery-2026-09-21/`:

- `CSS-Eve-BlackPearl-checkpoint.zip`, installable from the MortalShell2 root.
- `CSS-Eve-BlackPearl-checkpoint.zip.sha256`.
- `README.txt`, installation, controls, requirements and outstanding work.
- `Eve-motion-preview.mp4`, a separate offline movement study.

The ZIP uses the exact `css_core-choices1.dll` from the recorded live test,
with the installed loader verified against its five source-contract hashes
at commit `5f6bb73344d80a33c5ce2c014fd14d9c885f44fe`. The loader in the older
public v0.4.2 ZIP differs, so it was not substituted. A verified copy of the
actual matching loader is preserved in `work/delivery1/inputs/`.
This is a development checkpoint, not a new shipping build or tagged release.

Eve's three containers match both the previously accepted package receipt and
the current installed files. Package identity remains `eins0fx.seduxtress`,
variant `black_pearl`, version `1.0.0-candidate`; the public name remains
Eve (Stellar Blade), Black Pearl. CSSX, personal state, cached resources,
diagnostic requests and unfinished animations are excluded by an explicit
11-file payload allowlist, plus the checkpoint manifest.

`tools/authoring-probes/release/checkpoint.py` verifies both x64 DLLs, exact
payload hashes, ZIP member names and CRCs, complete byte readback, and the
extracted outfit manifest/resources/containers. Relative member paths stay
below 140 characters. The completed verification is in
`work/delivery1/verification.json`. Archive SHA-256:

`4d60c4abec7cbbfbd453fea07e7c0d806a540f9cdfa6137f0daebd2108731106`

The video shows M1 forward walk/jog/sprint with accepted fitted geometry and
secondary rigs, using 37 renders per gait at 15 fps. Its 2.47-second excerpt
repeats three times. The encoded MP4 has 111 frames, 1350 x 580 resolution and
7.4 seconds duration; ffmpeg exits 0. Two decoded video frames were visually
inspected. This is sampled visual inspection, not full-motion acceptance.
Cadence is provisional and the owner is stationary; no ground or gameplay
validation is implied. On-screen labels state that it is not installed.

No restart, deployment, settings change or external publication occurred.
The user's current installation already has this baseline. The newer Default
label and expanded animation support remain in source, outside this tested
binary. Full v1.0.0 scope, live animation acceptance and the modder kit remain
unfinished. Resume details for the paused phase correction are in
[the animation checkpoint](eve-animation-sources.md#phase-correction-checkpoint-p1-paused-for-delivery).
