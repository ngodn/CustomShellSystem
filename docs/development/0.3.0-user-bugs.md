# User reports to verify before 0.3.0

Collected 15 September 2026. User reports are symptoms, not established causes. Preserve these alongside the Cheat Menu parity and release-deliverables checklists.

Work order: finish the native Cheat Menu port already underway, then address this queue, then perform the final 0.3.0 release checks. New reports are notes, not instructions to interrupt the active task, unless _eins0fx explicitly changes priority.

| Report | Current status | Required check |
| --- | --- | --- |
| Equipped weapon attacks look unarmed | Fixed in the active session, confirmed by _eins0fx | Keep testing recurrence of completed-intro restrictions; preserve legitimate story locks |
| Teleporters, launch/jump points and death reset appearance | Empty-MID recovery stall fixed and live-tested; natural transitions still pending | Exercise same-pawn and new-pawn transitions with an active CSS selection |
| Bloody characters return to vanilla colors or broken materials | Investigating | Trace gameplay-created material instances and preserve blood/effect parameters alongside CSS colors |
| Eredrim's diapason and Tiel's dagger float under the feet after switching shells | Tiel attachment defect reproduced | Seductress has the socket but lacks its `tiel_dagger` bone (GetBoneIndex=-1); stock Tiel has bone 82 parented to thigh_l. Eredrim still needs comparison |
| Repeated shell hotkeys from original Cheat Menu eventually stop CSS | Not reproduced yet | Repeat gameplay-shell transitions and inspect player identity, menu lifecycle and recovery state; do not presume incompatibility |
| Seductress fabric behavior | Asset review pending | Separate cloth/skin weighting problems from runtime pause and attachment state |
| Seductress feet float during sprint and footstep sounds are missing | Asset review pending | Compare original and Seductress on the same gameplay shell and animation, including foot bones, notify tracks and physical material |
| Seductress and Beaute Genessa leave a long shadow after dodging | Not reproduced yet | Compare cloth deformation, bounds and shadow rendering on the same shell and dodge animation |
| HIT2 DE Scyther aims Trial Repeater into the sky | Not reproduced yet | Compare original replacement and CSS conversion with the same gameplay shell, weapon and aiming inputs; original-mod cause remains unconfirmed |

The original Cheat Menu should be disabled before testing CSSX Cheat Menu. CSS appearance compatibility with gameplay-shell changes still needs its own checks independently of that recommendation.

Local evidence before pausing this investigation: `work/cssx-native/tiel-missing-bone.json`, `tiel-before-css.json`, `tiel-css-sockets.json`. The blood-effect test preserved Seductress mesh/materials (`blood-live-check.json`). A simulated stock-mesh dynamic material delayed recovery until removal and then recovered (`transition-mid-repro.json`); that alone does not establish the natural portal failure's cause. The test selection on Tiel was removed and the gameplay shell returned to Genessa afterward.

15 September update: native Cheat Menu implementation is committed in `52168dd`.
The empty dynamic-material recovery stall now passes three repeated live cycles,
including active-effect protection and native Inventory cleanup. See
[the recovery notes](../beacon-transition-recovery.md) and
`work/cssx-native/material-recovery-live.json`. This is a constituent fix, not a
claim that every reported travel/death path is verified.
