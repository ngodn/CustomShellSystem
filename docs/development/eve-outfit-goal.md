# Eve outfit goal and current status

Updated 2026-09-26, takeover session 1. Read [the evidence index](eve-outfit-index.md) before resuming.

## Active objective

Deliver `CSS_EveStellarBlade_eins0fx_P` with all available Eve outfit variants, properly fitted to the original body proportions, complete applicable clothing customization, and verified clothing and secondary-motion physics. Every outfit needs **Original plus at least five clothing color palettes**. Hair-only recoloring does not satisfy this requirement. The user clarified this on September 26 and reports the latest Gemini outfit work is incomplete.

The user supplied this updated objective and resumed the goal through the conversation goal control. The September 26 fitting milestone is active; do not revert to the older quota-pause instructions.

**Latest priority correction: fitting first.** The user explicitly asked to focus on fitting every outfit to the same standard as Black Pearl. Palette implementation is parked until the fitting foundation is sound. Start with Holiday Reveler, then apply the proven process to the other outfits. Keep all customization requirements queued.

Claude Code owns CSS runtime UI/UX and performance work. This effort owns Eve assets, authoring scripts, verification and documentation. Do not stage, revert or replace the other agent's native changes. Current beta source, local release artifacts and installed binaries must be checked separately; an old installed `release.json` is not reliable version evidence.

Use Gemini's improved custom skeleton as the current baseline, as the user explicitly reminded us during takeover. Gemini's `disk_verification_report.json` records **386 bones, 82 sockets and nine virtual bones** after a fresh editor reload. Its seven additions are `Eredrim_Diapazon`, `Eredrim_Shoulder_l`, `tiel_dagger`, `unrealHelmet1_M`, `unrealBarrel1_R`, `weapon_l` and `weapon_r`. Preserve socket names, parents and transforms as well as the prior camera/hand fixes. This is existing editor evidence, not a new verification of the installed cooked skeleton. Do not rebuild from the older 379-bone baseline or overwrite current shared assets with an old release.

## Acceptance for every outfit

- Preserve the original body proportions and accepted hand, camera, movement and contact repairs.
- Original restores authored textures. Five or more additional palettes visibly recolor the garment, with useful coordinated colors and distinct results.
- Expose applicable garment colors and independent clothing toggles. Shared material sections must not accidentally hide another garment or part of the body.
- Preserve separate skin/anatomical controls and their masks. Clothing palettes must not recolor those regions or silently redirect to hair.
- Clothing fits at neutral settings and across supported body morphs. Check shoulders, underarms, chest, waist, hips, knees and footwear during motion.
- Cloth, hair and body physics behave under idle, locomotion, turns, attacks and transitions. Check collision, excessive stretching, clipping and resets; a successful commandlet log alone does not establish this.
- Profile save/load, switching outfits and Original reset preserve the correct settings independently.
- Review actual game screenshots and short movement recordings. Offline renders support diagnosis but do not replace game verification.
- Package under `/Game/CSS/`, use short readable paths, retain source provenance, and commit by coherent change. Do not publish until the candidate has passed these checks.

## Latest checkpoint

**Current result: animation-follow preserves the fitted baseline, but broader sprint checks expose real rear-hem clipping.** Independent hem controls reduce one measured pose's deepest penetration from 4.30 cm to 0.86 cm; this is a static feasibility test, not dynamics or acceptance. The remaining upper-hip penetration in that pose is the swinging left forearm. Excluding arm triangles leaves no upper-hip intersections deeper than 1 mm against the torso/legs, for default and combined hip/waist. Do not enlarge the fitted hip to compensate for forearm contact. The original carrier/component baseline still passes, both AnimDynamics candidates remain rejected, and source/game/release assets are unchanged.

F11's local hip repair now passes fresh export verification through the corrected weight cleanup. `holiday-base-clean.mesh.json` and `holiday-hip-clean.mesh.json` have identical influences; baseline geometry matches the earlier baseline exactly. The candidate preserves body points and all 22 morphs. `hip-source-verified.json` records the measured tolerances. These are private partial-outfit exports, not installed assets.

The repaired export was combined with the existing skirt-bone trial in `holiday-hip-bones.mesh.json`: 4,963 garment vertices use the existing 12 skirt bones, with zero discarded weight. `hip-bones-views/` front, combined hip/waist front and side renders were reviewed. The previous visible hip patch is gone; the trim remains attached under the synthetic four-degree bone bends. Nine local morph grid checks in `hip-clean-morphs.json` also pass. This is not a simulated motion/collision test or a full-body clipping guarantee.

Next validate actual skirt motion while preserving the current secondary animation graph, then complete the whole-outfit assembly and game fitting review. Hair, hat, earrings and footwear remain outside these partial exports and must not be forgotten. Do not replace the source graph with a newly generated graph that omits Gemini's body/hair physics or accepted idle behavior. Keep independent body-morph support; the panel-cloth single-active-morph limitation remains unresolved.

The user's question about time spent on Holiday was explicitly curiosity, not a priority change. Keep the existing fitting and clothing-motion plan, avoid reopening settled experiments, and retain all outfit/customization requirements.

## Current evidence

| Capability | Finding | Evidence |
| --- | --- | --- |
| Outfit catalog | Eleven variants exist in generated and installed manifests | `tools/eve_source_audit.py`; local `work/eve26/installed-manifest.json` |
| Black Pearl palette wiring | Six palettes target clothing controls; visual quality remains to be rechecked on current build | Installed manifest |
| Ten added outfits | Each has five palette entries, but all target only hair and hair accessories | Installed manifest; generator diagnosis below |
| Clothing toggles | Some controls share material sections; intent and independent visibility need mesh review | Audit JSON, export material lists |
| Eve animations | Installed definitions include I2 idle and S1 walk/jog/sprint | Manifest definitions, not new playback verification |
| Holiday fitting | Existing Blender renders reviewed; fitting and render/export agreement still need work | Gemini fitted-check renders and scripts |
| Physics | New authoring/runtime work exists; per-outfit cooked configuration and gameplay behavior not yet accepted in this takeover | CSSBindClothV2/V3 sources and Gemini bind scripts |

Known outfits: Black Pearl, Vacation Bikini, Casual Knitwear, Holiday Reveler, Midsummer Alice, Ocean Maid, Planet Diving 6th, Royal Guard, Skin Suit, War Aegis and YoRHa No.2 TypeB. Inventory additional source outfits before declaring this list exhaustive.

## Confirmed palette defect

`CSS-Mod-Authoring/eins0fx-collections/CSS_SeduXtress_eins0fx/gemini-work/generate_variants_manifest.py`:

1. `build_variant_controls` removes clothing color controls for added outfits.
2. `make_surfaces` constructs only body and hair dye surfaces.
3. `generate_manifest` copies `suit_color` to `hair_color`, then drops values whose controls were removed.

The installed manifest confirms that result for all ten added outfits. This explains why valid-looking palette names and a count of five did not provide outfit customization. Removing the fallback alone is insufficient: supply reviewed garment material bindings and suitable dye layers, then retain the garment palette values.

Additional risks to inspect: Holiday top/bottom/accessory toggles share sections; the generic hair assignment uses object-name matching and arbitrary first-slot splitting; material staging forces every base-color alpha to opaque. These are source findings, not proof of every visual defect reported by the user. Do not copy Black Pearl's UV masks to unrelated outfit atlases.

The added meshes also have a different material order from Black Pearl. For example, Holiday export slot 2 is `Eve Legs`, while the generator applies the Black Pearl chest/body masks there; slot 5 is `Eve Fingernails`, while its dye surface uses the groin masks. Resolve surfaces by material identity and verify cooked bindings before deployment. The existing `inspect_materials_out.txt` captures an earlier unbound Ocean Maid state, so do not mistake that old WorldGridMaterial listing for the currently installed material state.

## Next implementation steps

1. Repair Holiday Reveler's fitting foundation in an isolated candidate, preserving Gemini originals. The `Variants_Fixed` blend inventory shows all 7,600 sleeve vertices, 3,347 leg-piece vertices, 3,401 underwear vertices and 7,044 Christmas heel vertices without any group assignments. Of 16,496 dress vertices, 14,188 are unweighted. These counts describe that saved blend, not necessarily the earlier cooked game mesh. Establish which source/export is currently installed before replacement.
2. Reuse Black Pearl's body-preserving method: garment-only fitting, stable triangulation, surface-aware weight transfer, supported morph clearance and motion checks. Inspect evaluated shape keys and bind transforms before projection. Validate preserved body geometry and the current 386-bone/socket skeleton; do not globally shrink the body or use indiscriminate nearest-vertex transfer.
3. Compare front, back and side renders, then motion/contact behavior. Keep dress shape, trim and intended loose regions intact. Extend the verified fitting process to remaining outfits, including cloth collision and physics.
4. After fitting, repair name-based material mappings, independent toggles and outfit-specific dye layers; deliver five clothing palettes plus Original and per-outfit save/load checks. Keep accepted Black Pearl behavior as a regression baseline.

Local diagnostic outputs are under `CustomShellSystem/work/eve26/`; source assets remain in the authoring collection. No new asset candidate was installed during the takeover audit. The previous quota-era queue entries below September 26 are history, not the current build state.

## Holiday F1 investigation

The original `eve_beta10.blend` and the authoring master both retain full Holiday dress/sleeve skin weights. The current `Variants_Fixed` has lost them. Gemini's fitting script clears all groups and applies Data Transfer without generating destination layers. Blender documents that the modifier does not create those layers: https://docs.blender.org/manual/en/4.3/modeling/modifiers/modify/data_transfer.html. The saved dress also has a 4.98 mm maximum discrepancy between mesh coordinates and its shape-key Basis, while the source has none.

The original sleeves have the same coordinates as the broken candidate, although the target body's arm pose differs from the original body. Weight repair alone therefore cannot establish a correct fit. F1 reconstructs four garment objects from the intact master using matching body topology and surface correspondence, preserving loose cloth offsets. It directly creates normalized CSS skin weights and keeps shape-key coordinates consistent. Body geometry, morphs and weights are hashed before/after; the Blender rig is checked unchanged. This is a separate offline candidate, not an installed repair.

Probe: `tools/eve-fit/probe_sources.py`, results: `work/eve26/fit-probe.json`. Candidate builder: `tools/eve-fit/holiday_candidate.py`; neutral-material review: `tools/eve-fit/render_holiday.py`. Pending acceptance includes visual silhouette/clearance, morph endpoints, deformation, cloth setup and installed cooked-skeleton preservation.

F1 outcome: all four rebuilt pieces have complete weights and unchanged body/rig checks pass. Front and side review, including the exporter-equivalent no-modifier view, still show chest penetration and distorted sleeves. **Rejected for deployment.** Surface correspondence alone is insufficient. Next inspect the pristine source's evaluated armature/surface-deform/corrective stack and reproduce its intended garment shape before adapting it to the CSS body; do not repeat F1 with arbitrary clearance increases.

Pristine source visual check: `work/eve26/original-visible/front.png` shows a covered chest, correctly shaped collar and coherent loose sleeves when the original deformation stack is retained. The initial `original-views` capture was invalid because wardrobe visibility drivers hid the outfit. The review tool now overrides visibility drivers and links requested pieces into a visible collection without saving the source. Source evaluation logs warn about eight dependency cycles, so explicitly validate the evaluated mesh and modifier dependencies before baking. Next candidate should start from that evaluated garment shape, not raw mesh vertices. Preserve the CSS body rather than importing any outfit-driven source body deformation.

## F2 evaluated-source fitting

`holiday-f2.blend` uses the pristine original, with render-time modifier enablement mirrored into a fixed viewport evaluation, source body/garment topology checked unchanged, and the target CSS body's saved shape-key mix. All four rebuilt parts have complete normalized weights. Body digest remains `3b0dc6afa992ee9921da702d135341856c5695ef51805412a7a99ffd3234d1ec`; the Blender rig is unchanged. The shared Unreal skeleton was not written.

Reviewed `work/eve26/f2-bind/{front,side,back}.png`: the large chest breakthrough from F1 is gone, the collar retains its intended shape, and sleeves are substantially more coherent. A small sleeve skin breakthrough remains visible from the back. This is a better static fitting baseline, not a finished outfit or game-verified asset. Source particle fur is deliberately omitted from these neutral-material checks.

The bounded F3 clearance attempt (`tools/eve-fit/holiday_clearance.py`) stopped before saving: dress iteration zero found 5,303 penetrating triangle/edge/vertex samples within an 8 mm nearest-surface radius; the proposed move exceeded its 8 mm displacement budget. No `holiday-f3.blend` was produced. These are repeated sample counts, not unique vertices, and signed nearest-surface tests can misclassify concave regions. Inspect affected material regions/locations before increasing clearance or changing the silhouette. Do not call F2 intersection-free based on the renders.

Next: locate the clearance violations, correct the sleeve breakthrough and any genuine dress penetration, then transfer/verify supported CSS morphs against the preserved body and test deformation. F2 currently retains transformed source shape deltas with baked default values; those deltas are not accepted as CSS morph conformance. No game installation or release replacement yet.

## Outer-surface clearance and morph preparation

Material-region probe `work/eve26/clearance-regions.json` separates fur/inner-wall warnings from clothing fabric. The `--outer` clearance mode excludes fur roots and faces opposing the nearest body normal, uses 2 mm steps and a 12 mm movement bound. Dress sampled violations fall from 603 to zero in three iterations; underwear from 1,103 to zero in two. Sleeves and leg pieces reach the movement bound with unresolved samples, so this is not whole-outfit acceptance.

**Discard F3b as a fitting baseline.** A fresh morph-build precondition detected mismatch between saved mesh coordinates and shape-key Basis. The clearance writer read key coordinates after modifying the mesh, allowing an offset to be applied twice. F3c snapshots keys first but its exact Basis comparison failed; the next writer constructs Basis directly from fitted coordinates and preserves other key deltas relative to it, with an input alignment check. No game deployment occurred. F2 remains the last visually reviewed baseline until a corrected save passes fresh reload.

`tools/eve-fit/holiday_morphs.py` transfers the six manifest-supported controls (range 0..1) from the preserved CSS body to garment surfaces. Its first run stopped before saving, correctly catching the F3b mismatch. Endpoint and combined-maximum rendering is prepared in `render_holiday.py --morph-review`; it is not yet acceptance evidence. Use Blender `--python-exit-code 1` for subsequent CLI runs, since otherwise script exceptions can return process exit zero.

### Corrected save and F4 morph review

F3d saved after explicitly constructing Basis from fitted coordinates and preserving other keys relative to the captured Basis. The subsequent fresh-load morph build passed exact mesh/Basis checks on all four pieces and produced `work/eve26/holiday-f4.blend`; the body hash remains unchanged. All six body controls were transferred at their manifest-supported 0..1 range.

`work/eve26/f4-morphs` contains 18 renders: four default views, front/side for each individual maximum and front/side for the combined maximum. Reviewed default back, chest maximum front and combined front/side. The visible rear sleeve skin patch from F2 is gone and garters look cleaner at default. **Chest maximum exposes a center split/pinch in the upper band**, plus a tiny front fabric breakthrough. Morph transfer is not visually accepted. Inspect duplicate/seam vertex correspondence and displacement continuity before applying further clearance; do not change the body or hide it to cover the defect. Other individual views were generated but not yet all visually reviewed.

Current best offline candidate is F4 for continued work. Its underlying F3d bounded sample results: Dress passes (max move 5.32 mm), underwear passes (2.08 mm), Arms retains 186 sub-clearance samples at the bound (10 mm accumulated move), Legs retains seven (12 mm). These sample limitations remain despite the improved rear render. Motion, cloth simulation, cooked shared-skeleton preservation and game acceptance remain open. No candidate has been installed.

### F5 morph continuity correction

The seam probe found no expanding gaps among dress vertex pairs within 0.1 mm at rest. It did find connected edges stretching sharply: one edge grows from 0.878 mm to 29.36 mm under `PBMBreastsSize`. This supports a discontinuous transferred morph, not changed garment topology. The proximity check does not rule out wider disconnected seams.

`holiday_morph_continuity.py` limits differences between neighboring garment morph offsets while preserving topology, the default fit and all body data. F5 converges after 289 iterations; maximum connected-edge stretch falls from 33.45 to 2.00. This is a geometric bound, not an aesthetic acceptance threshold.

Reviewed F5 chest maximum front and combined front/side against F4: the central opening closes, but a center crease and a tiny skin-colored point remain. Other individual morph front/side views reviewed so far retain the overall garment silhouette. Default back and quarter views retain loose sleeves and skirt shape. F5 is the current continuity baseline, still offline and not accepted for deployment. Sleeve/leg clearance limitations from F3d remain.

The clearance tool now supports a single morph endpoint and garment part. That mode writes only the selected garment key and asserts that the base, other keys and body remain unchanged. F6 is a chest-only dress clearance trial from F5; its result must be freshly loaded and visually checked before acceptance.

F6 fresh reload and rendering completed successfully. Reviewed chest-maximum front and combined-maximum front: the tiny skin-colored point visible in F5 is gone, and the central opening remains closed. Maximum additional garment displacement is 0.865 mm. The central crease remains visible, so do not describe the maximum-size surface as perfectly smooth. F6 is the current offline fitting baseline. Next inspect the remaining sleeve/leg clearance regions and garment deformation through actual poses, then clothing physics. No game installation or release replacement has occurred.

### Cuff and garter diagnostics

F6's seven-sample outer-fabric region probe reports 187 sleeve and 11 leg sub-clearance samples at the saved default (sample counts can differ slightly from in-memory fitting iterations). Highlighted front/back garment-only renders in `f6-regions` show the worst reported faces mainly at cuff openings and inside the garter. The diagnostic deliberately hides the body to expose internal faces; this is not a fitting workaround or an exported change.

F7 applies another bounded pass to Arms and Legs only. **Rejected as a working baseline:** sleeve failures fluctuate around the original count and again reach the displacement bound; legs also reach the bound with unresolved samples. Do not continue increasing this correction budget. Keep F6 while checking surface containment and folded geometry. `probe_clearance.py --rays` records six-direction ray crossings and first-hit normals for the worst samples to distinguish actual body containment from nearest-surface ambiguity. Motion and cloth validation remain pending.

F6 ray results (`f6-rays.json`) support genuine internal intersections: the 32 sampled entries across four material/body-region groups have odd crossing counts and exiting first-hit normals in all six directions. Some entries repeat triangle vertices, so this is not 32 unique locations. Do not dismiss these as signed-distance false positives. Next inspect the intersecting faces' topology, including possible internal closing faces or long triangles spanning cuff/garter openings, before deciding between local refitting and topology repair. That topology explanation is still a hypothesis, not a finding.

The unrestricted nearest-body vertex probe (`f6-internal.json`) identifies deep triangle-fan hubs: sleeve vertex 1 has 48 neighbors and is about 24 mm inside the body; garter vertex 3321 has 56 neighbors and is about 71 mm inside the thigh, with edges up to 90 mm. All faces incident to these hubs are triangles. The earlier 8 mm sampling radius cannot see the deep hubs and only encounters their triangles near the skin. This explains why that metric could not establish whole-garment clearance. There are also low-degree internal vertices, so removing fan caps alone may not resolve every intersection.

`holiday_open_caps.py` prepares a separate F8 candidate by removing faces incident to internal triangle-only fan hubs (at least 20 neighbors, more than 8 mm inside, longest edge over 25 mm). It preserves vertices, all shape-key coordinates, weights, and surviving material/UV assignments with exact assertions. The body is hashed unchanged. This is a targeted topology trial, not an accepted export. Blender documents the face-only operation in its [BMesh API](https://docs.blender.org/api/5.1/bmesh.ops.html#bmesh.ops.delete); preservation is checked against the actual installed Blender, not assumed from the API.

F8 completed and passed those assertions: 13 sleeve fan hubs account for 449 removed faces; five garter hubs account for 198. Unused hub vertices remain intentionally to preserve all vertex indices and shape-key correspondence. Fresh reload region checks reduce outer-fabric sub-clearance samples from 187 to 46 for sleeves and 11 to four for legs. Reviewed fresh front/back renders retain the visible garment silhouette. The unrestricted vertex probe still finds 21 sleeve and 95 leg vertices deeper than 8 mm, excluding unused hubs; these remaining inner-wall regions are not cleared by the cap removal. F8 is the current topology baseline, pending remaining fitting/morph/motion checks. F9 tries the bounded outer-surface correction again on this changed topology, not a larger budget on F7.

F9 converges after cap removal: Arms reaches zero sampled outer-fabric failures after seven correction steps, Legs after six. Maximum additional moves are 8.43 mm and 8.41 mm respectively, below the existing 12 mm bound. Body hash remains unchanged. This proves that the capped topology obstructed the prior bounded pass; it does not prove every inner surface is clear, nor that motion and physics are finished. Fresh saved-file renders are the next visual gate.

Fresh F9 renders completed. Reviewed default back/quarter and combined-maximum front/side: loose sleeve and skirt silhouettes remain coherent, garters remain fitted, and the earlier chest breakthrough does not return in these views. F9 is now the working candidate for pose/motion validation. Remaining inner-wall intersections require inspection in deformation; passing the restricted outer-fabric sample test is not whole-mesh clearance. Existing `tools/authoring-probes/animations/render_eve.py` provides the verified UE-pose replay math, but is hardcoded to old Black Pearl assets/379 bones and removes covered body sections. Adapt a separate Holiday review tool with explicit rig/bind compatibility checks and visible body geometry; do not overwrite the current 386-bone game skeleton or treat the old replay as current game acceptance.

### Recorded pose replay

`tools/eve-fit/replay_holiday.py` checks the candidate's authoring bind against `work/anim10/batch/target-bind.json`, then applies recorded component transforms with the existing verified UE-to-Blender matrix conversion. It renders the full body and Holiday pieces without saving the asset. The authoring rig has 379 bones; the shared current Unreal skeleton remains untouched at 386. This is an offline garment deformation test, not validation of the current cooked skeleton or cloth simulation.

`work/eve26/f9-idle` replays frames 0, 120 and 240 of the recorded I2 idle (`work/anim16/center-component.json`). Maximum reconstructed bone-position error is 0.000286 cm, with zero reported angular error; source blend hash is unchanged. Reviewed frame 0 front, frame 120 back and frame 240 side. The skirt hem develops sharp steps under the pose despite the clean static fit. Do not accept F9 motion. Measure garment edge deformation and skin-weight transitions, then correct the garment weights without changing the body or animation. The renderer now reports the eight most stretched edges per garment/frame to guide that investigation.

`f9-walk` replays frames 0/10/20/30 from the accepted S1 walk recording (`work/anim14/walk22-component.json`). Reviewed frame 10 front, 20 side and 30 back: pronounced stepped skirt hem and sharp fabric folds persist. At frame 20, connected dress edge 2390/2393 grows from 8.89 mm to 140.57 mm; underwear edge 1399/2361 grows from 2.68 mm to 48.31 mm. Sleeve edges also stretch about six times, while garter edges remain close to their original lengths. These results direct the next repair toward skin-weight continuity on Dress, Arms and Panties.

`holiday_weight_continuity.py` prepares F10: bound changes in the bone-weight vector between connected fabric vertices, adding links for coincident seams within 0.1 mm. Only edges belonging to surviving faces are used, excluding the orphan radial edges left by cap removal. Preserve mesh and morph coordinates and body data exactly. The 15-per-metre gradient bound is a trial parameter, not a release standard. F10 requires comparison against the same recorded poses before acceptance.

F10 saved with body/geometry/morph preservation assertions passed. Dress reaches the 500-iteration limit with 1,404 edges still above the trial tolerance (maximum gradient 17.16 versus target 15); do not claim full numerical convergence. Fresh `f10-walk` replays the same four S1 frames and passes bone reconstruction checks. Across those frames, maximum edge stretch falls from 17.47 to 2.45 for Dress, 6.25 to 1.50 for Arms, and 18.06 to 1.72 for Panties. Garter results are unchanged. Reviewed matching frame 10 front, 20 side and 30 back: abrupt hem steps are reduced and fabric transitions smoother, but the skirt still follows leg motion too strongly. F10 is the current weight candidate, not finished clothing motion. Next refine skirt deformation/cloth anchoring, replay idle/jog/sprint and combat poses, and inspect remaining inner-wall contacts. No asset has been installed.

### Clothing simulation path

The supplied Stellar Blade cloth-guide transcript describes bone-driven secondary motion using a compatible source skeleton, garment-only bone weights, a physics asset and post-process animation. It is not a Chaos mesh-cloth recipe and cannot be copied directly onto the CSS skeleton.

Fresh UE 5.6.1 read-only audit (`tools/eve-fit/audit_ue_cloth.py`, `work/eve26/ue-cloth.json`) loaded saved Black Pearl, Holiday and YoRHa meshes. All three reference `/Game/CSS/Shared/SKEL_Base`, `PA_Body` and `ABP_Secondary`, but have empty `mesh_clothing_assets` arrays. This proves the saved editor meshes have no bound Chaos cloth assets; it does not rule out bone-based secondary motion or establish the installed package contents. Gemini's variant bind script uses `CSSBindClothV2 -SkeletonOnly`, which must not be interpreted as garment cloth creation.

An existing separate `CSSBindClothV3Commandlet.cpp` in `CSS-eins0fx-collections/tools/CSSAuthoring/Source/CSSAuthoring` supports per-slot simulation proxies, collision Physics Asset, Max Distance maps and configurable Chaos properties. The v202 pipeline uses it. Its default masks and slot names are Seductress-specific, so use an isolated Holiday mesh and explicit garment configuration rather than applying defaults to the shared production mesh. Epic documents Max Distance relative to the animated skinned position and separate collision configuration in the [Clothing Tool reference](https://dev.epicgames.com/documentation/unreal-engine/clothing-tool-in-unreal-engine---properties-reference). Keep garment skinning coherent as the simulation's fallback, and validate collisions in the matching editor before cooking. No source assets were saved during the audit.

F10 additional pose checks: `f10-jog0` and `f10-sprint0` replay frames 0/10/20 of accepted S1 component recordings. Both runs complete bone-transform checks. Reviewed jog frame 10 front/20 side and sprint frame 10 front/20 back/side. No return of the previous extreme edge discontinuities in those views, but the skirt still lifts and bends strongly with the thighs. Sprint's maximum measured dress-edge ratio reaches 4.06, so this is not full deformation acceptance. Next stage a separate Holiday UE candidate from F10, preserving the existing shared skeleton and body secondary assets, then author skirt-specific simulation geometry, anchor weights and collisions. Broader combat/morph/cloth checks remain required.

Baseline clarification for future sessions: F2's authoritative receipt names `gemini-work/CSS_SeduXtress_Variants_Fixed.blend` as the target baseline, and pristine `eve_beta10.blend` as the garment source. F10 preserves that target body's digest. Do not confuse the original/master comparison probe with the candidate's actual target body.

### Isolated Holiday cloth-development import

`tools/eve-fit/export_holiday.py` exports F10's body, Dress, Arms, Legs and Panties through the existing exporter, preserving its hand correctives and six body morphs. The saved blend SHA-256 is checked unchanged. Hair, hat, earrings and footwear are deliberately absent from this development mesh; this is not a complete outfit package. The exporter requires the original measured hair-transform audit even without hair, so the wrapper copies that existing audit into the workspace rather than fabricating it.

Export receipts are `work/eve26/holiday.mesh.audit.json` and `holiday.export.json`. The bind check reports 379 authoring bones and maximum reference-head error 0.000085 cm. The current commandlet accepts `/Game/CSS/`, unlike Gemini's old `prepare_mesh_import.py` helper which insists on `/Game/CSSAuthoring/`. Export directly to unused private mesh/skeleton packages instead of using that legacy helper or replacing shared assets.

Fresh read-only `audit_holiday_import.py` passed name/parent compatibility for all 379 imported bones against the saved 386-bone shared skeleton. The current shared skeleton has 82 sockets and nine virtual bones; its seven additional bones remain present. Receipt: `work/eve26/holiday-compat.json`. This checks hierarchy, not equal rest transforms or gameplay behavior. Imported material slot names retain Blender numeric suffixes and differ from the production Holiday mesh, so resolve materials by explicit source identity before binding cloth. The test mesh has no cloth assets yet.

`setup_holiday_materials.py` now assigns all 30 private test slots from the existing production Holiday materials, using 14 explicit garment aliases for appended-material suffixes. It also copies the current body physics, shadow physics and post-process references. `work/eve26/holiday-materials.json` records each assignment and hashes proving the production Holiday mesh and shared skeleton files stayed unchanged. Commandlet completed successfully. Appearance still requires a textured visual check and fresh saved-reference verification; existing materials may contain prior Gemini issues. The private mesh still needs shared-skeleton binding and cloth authoring.

Parallel Commander White handoff requested by the user: `CSS-Mod-Authoring/eins0fx-collections/CSS_CommanderWhite_eins0fx_P/AGENT-PROMPT.md` now contains source-audit instructions, references, Eve fitting lessons, skeleton/cloth pitfalls and shared-resource coordination. Commander White's original `reference/CW.blend` remains untouched. Its folder is outside the detected CSS Git repository. Continue Eve fitting here; the other chat owns Commander White and Claude Code owns CSS UI/performance.

### Holiday cloth section structure

`probe_cloth_sections.py` inspects the exported triangles, not raw Blender edges. `holiday-sections.json` finds the main dress slot has 12,424 referenced vertices across 107 components; its largest component has 4,336 vertices spanning Z 94.26 to 137.88 cm. Fur has 3,120 vertices/three components; decal has 277/five; emissive has 337/three; the fifth material has 383/one. Main/fifth slots share 44 exact topology edges. Coincident disconnected seams are not welded in this measurement.

`render_cloth_sections.py` reconstructs the actual exported mesh and renders material-region colors without loading or changing the source blend. Reviewed all three `work/eve26/section-views/{front,back,side}.png`: blue main material covers bodice/skirt plus straps, bows and details; orange decal patches overlay skirt regions; red fur includes the lower hem, upper band and rear ornament. These are diagnostic colors, not palettes or textured material acceptance.

The existing V3 cloth commandlet creates a separate cloth asset per requested material section and pins each disconnected component at its own highest vertices. Applying that unchanged would not establish coherent movement for skirt, overlaid patches and hem. It also caps each simulation mesh at 5,000 vertices. Next derive the main skirt surface and attached trim/decals as a coherent simulation design, inspect matching UE source for shared simulation-to-render section mapping, and preserve original render materials. Do not just decimate each material independently and label it finished cloth. Existing v202 proxy builder is an example of per-component decimation and loose-vertex validation, not a solution to this cross-material attachment problem.

Installed UE 5.6.1 source confirms `ClothingAsset.cpp:201-222` rejects an already mapped cloth LOD in `BindToSkeletalMesh`. Its section mapping at lines 230-348 stores `CorrespondClothAssetIndex` and mesh-to-mesh weights. `SkeletalRenderGPUSkin.cpp:1008` selects simulation data by that section asset index. This supports investigating an explicit shared render-mapping implementation; it does not prove it survives rebuild, cook or runtime. The [Epic API reference](https://dev.epicgames.com/documentation/unreal-engine/API/Runtime/ClothingSystemRuntimeCommon/UClothingAssetCommon) describes the public binding surface; use the installed source for this version's actual guards. Do not clear LodMap temporarily just to bypass the guard.

`extract_skirt_proxy.py` writes `work/eve26/skirt-surface.json`: the main connected dress panel, 4,336 vertices, 8,328 triangles, 346 boundary edges and no edges shared by more than two triangles. This is an undecimated candidate, with original normalized skin weights. Anchors, leg-weight decoupling, attachments and visual simulation remain pending. Initial cross-product normals were rejected after comparison with exported normals showed the UE clockwise winding reverses their direction. The current candidate averages the original exported corner normals instead; rejected output is retained as `skirt-crossnormals-rejected.json`. No original asset or game file changed.

### Proxy leg-weight trial

`test_skirt_weights.py` transfers thigh/descendant influences to pelvis on the proxy only, affecting 901 of 4,336 vertices. Geometry and other bone weights are retained. It evaluates linear skinning from exported bind transforms and recorded walk/sprint poses (frames 0/10/20), writing `work/eve26/skirt-weights/pelvis-proxy.json` and the comparison report/renders. This bypasses Blender armature replay and omits morphs, body collision and simulation, so treat it as a weight-isolation experiment rather than final garment evidence.

Across the sampled sprint frames, original main-panel maximum edge ratio reaches 3.183; trial maximum is 1.531. At sprint frame 20 it changes from 3.183 to 1.354. Walk frame 20 improves from 1.612 to 1.414, although frame 0 slightly worsens from 1.376 to 1.391. Reviewed sprint frame 20 original/trial front and trial side: the strong one-sided hem lift is reduced while the bodice remains posed. This supports leg decoupling for the simulation target, not acceptance without collision. Next define anchors on the upper fabric and validate coherent trim mapping and collision in the actual simulation. The rendered outfit, source blend, shared skeleton and game installation remain unchanged.

### Anchor trial and saved collision audit

`prepare_skirt_anchors.py` creates `skirt-config.json`, `skirt-proxies.json` and `skirt-anchors.json` from the pelvis-weight trial. The explicit V3 settings use Anchor=20 cm below the panel top, Falloff=12 cm and maximum displacement=18 cm. This pins vertices at/above Z 117.884 cm and reaches full allowed motion at/below 105.884 cm: 3,070 fixed and 1,266 movable vertices. These are initial trial parameters, not validated simulation results. The proxy/config remain unbound.

`audit_holiday_collision.py` freshly reads `PA_Body` using the existing C++ reflection probe and records all 22 body setups in `work/eve26/holiday-collision.json`. It also confirms the private Holiday mesh saved the production physics and post-process references, while retaining its private import skeleton. The pelvis contains a sphyl radius 20.109 cm/length 13.115 cm plus a tapered capsule; thigh sphyl radii are about 11.884 cm with 34.884 cm lengths. Those dimensions are not proof of bad gameplay collision, but cannot be assumed suitable for close cloth. Next transform the actual collision shapes into mesh space and compare them with garment/body surfaces, then use a separate cloth collision asset if required. Preserve `PA_Body` and existing gameplay/body secondary behavior.

`probe_skirt_collision.py` reconstructs exported bone component transforms and capsule endpoints using the installed UE `RotationTranslationMatrix.h` rotated-UpVector formula. Chaos's `ChaosClothingSimulationCollider.cpp:604-609` extracts sphyl endpoints as center plus/minus rotated UpVector times half length. Probe checks near-unit bone scale and uses that same capsule convention. Saved result `skirt-collision-fit.json`: all 1,266 movable proxy vertices lie inside the pelvis sphyl at rest, maximum penetration 11.619 cm across the panel. Left/right thigh sphyls contain 159/115 movable vertices, with maximum penetration 4.620/5.068 cm. Scope excludes tapered shapes, other bones, animation and added collision thickness.

Reviewed `collision-views/front.png` and `side.png` from `render_skirt_collision.py`: the wire pelvis capsule extends substantially beyond the skirt's front/back silhouette, consistent with the analytic containment result. Existing `PA_Body` is therefore unsuitable as-is for this cloth trial. Preserve it for gameplay and body behavior. Next fit a dedicated cloth collision set against both body and garment, including moving poses; reducing radii merely to eliminate proxy containment would not prove adequate body coverage. No asset binding, source mutation, cook or deployment occurred in this diagnostic.

### Separate collision fitting trials

`fit_cloth_spheres.py` fits spheres inside the actual exported body using nearest-surface distance minus 0.15 cm, with inside classification requiring agreement of three ray directions. Initial pelvis/thigh seeds yield 16 accepted spheres and reject the central seed at Z105 (outside in all three directions). `cloth-spheres.json` has zero proxy overlap but poor body coverage: median surface-to-sphere gap 2.458 cm, p95 5.673 cm in the measured region (3,045 body vertices, Z92..118 cm, |X|<22 cm). This is rejected as adequate coverage.

The current script adds bounded adaptive spheres at poorly covered surface regions, capped at 32. `cloth-spheres2.json` improves median gap to 1.573 cm, p95 to 2.360 cm and maximum to 3.629 cm; no proxy vertices overlap, minimum skirt clearance 0.442 cm. It still misses the trial p95 target of 1 cm. Do not accept this as finished collision or keep increasing sphere count blindly. Next assess connecting compatible spheres with tapered capsules and pose/morph coverage, or fit a more efficient body approximation. Bone assignment for added spheres is a provisional geometric pelvis/thigh classification requiring animated validation.

Additional V3 pipeline issue: `CSSBindClothV3Commandlet.cpp:167-184` hardcodes the mesh gameplay physics reference to `/Game/Sparta/Characters/NPCs/SesterGenessa/Art/Mesh/PA_Sester_Genessa_V5_Capsules_NoCloth`, separate from its cloth collision `-Physics` argument. Before using it for Eve, add an explicit preserved mesh-physics option or an isolated binding path. Do not let cloth development replace Eve's accepted `PA_Body` binding. No commandlet source modification has been made yet.

`connect_cloth_spheres.py` measures tapered connections between same-bone sphere pairs. It rejects containment-degenerate pairs, axis-sampled body excess over 0.1 cm and skirt clearance below 0.2 cm, then greedily selects at most 16 connections improving surface coverage. `cloth-capsules.json` retains all 32 source spheres plus 16 connections. Median body-region gap improves to 1.379 cm, p95 to 2.206 cm and max to 3.475 cm. This modest improvement does not meet the previous trial target and is not acceptance. Worst-covered vertex coordinates are included for targeted correction. The envelope calculation minimizes distance to the linearly varying-radius sphere family; it still needs comparison with native Chaos extraction and animated behavior.

The installed Chaos collider source maps tapered capsules to two endpoint spheres plus a connection (`ChaosClothingSimulationCollider.cpp:650-664`), with Radius0 at center+half-axis and Radius1 at center-half-axis. Preserve that endpoint order when importing a definition. The existing `CreateBodyCandidate` helper is unsuitable for this new asset: it is restricted to 379-bone diagnostic meshes, a legacy `/Game/CSSAuthoring/DiagnosticReferences/PA_B2BodyFit` prefix, exactly 22 bodies and 21 constraints. Add a separately scoped cloth-collision authoring path rather than pretending this gameplay-physics helper accepts arbitrary cloth primitives. No native helper/source edits, binding or deployment yet.

### Private native collision asset

Added tracked `tools/eve-fit/native/CSSEveCollisionCommandlet.{h,cpp}` and staged identical copies into the shared authoring project's `Source/CSSAuthoring`. The separate command accepts unused `/Game/CSS/EveTest/PA_` outputs only, validates finite geometry, bone existence, same-bone connections and duplicate/degenerate pairs. It saves a new collision asset without assigning or saving a mesh or skeleton. Unconnected spheres remain standalone; connected ones are represented by tapered capsules with verified endpoint ordering. It does not modify the V3 cloth binder.

Build notes in `native/README.md`: UE 5.6.1/clang 18 build succeeds with `-NoUBA`. Initial UBA attempt chose `~/.epic/UnrealBuildAccelerator` outside the writable workspace and launched no compiler; explicitly terminated that verified process before retrying. An inspector compile error (`ToSharedRef` on an existing shared reference) was corrected. Final successful build log is `work/eve26/collision-build4.log`.

Created `/Game/CSS/EveTest/PA_Holiday` from the capsule trial. Creation exited zero. A Python readback attempt failed because `PhysicsAsset.skeletal_body_setups` is not exposed; no asset changes resulted. Added read-only `-Inspect` to the new C++ commandlet, then fresh native readback wrote `cloth-collision-native.json`. `verify_cloth_collision.py` passes: three bodies, 16 tapered capsules, 13 standalone spheres, matching bone identities and capsule radii/lengths/endpoints within 0.0001 cm. Receipt: `cloth-collision-saved.json`. Standalone sphere dimensions are recorded in the native report, but only their count is checked by this verifier.

`collision-protected.sha256` confirms the shared skeleton, production `PA_Body` and private Holiday mesh files stayed unchanged. The new collision asset is not bound to a mesh or cloth. Coverage limitations from the numerical trial still apply. Next implement isolated cloth binding with preserved mesh physics and coherent render attachments, then evaluate actual motion and collisions. No cook, installation or release replacement occurred.

UE import saved `/Game/CSS/EveTest/SK_Holiday` and `/Game/CSS/EveTest/SKEL_Holiday_Import`: 67,613 points, 119,534 triangles and 22 morph targets, with zero reported commandlet errors. Materials are placeholders. The shared 386-bone skeleton was not an import target. Next freshly inspect the imported asset, compare its bone hierarchy with the current shared skeleton, bind only the private mesh, and set up Holiday-specific cloth geometry/anchors/collision. Do not merge the older authoring skeleton over the current shared skeleton. The strong thigh-driven skirt motion is still unresolved; no game deployment or release replacement occurred.


### Isolated cloth binding experiment, not accepted

Added `tools/eve-fit/native/CSSEveClothCommandlet.{h,cpp}`, staged in the authoring project and compiled successfully (`cloth-build2.log`). This private-path binder preserves mesh gameplay physics and post-process references, checks skeleton names/parents, and tries to map four dress attachment material sections to one primary simulation. It does not modify the original V3 binder.

Both binding attempts exited before saving. The first incorrectly queried render clothing data before rebuilding; primary asset retrieval now uses the mesh clothing asset array. The second reached the rebuild but failed `Shared attachment lost during rebuild` (`cloth-bind2.log`). That check combines the render asset lookup and imported mapping presence, so it does not yet establish which failed. Do not treat this as working cloth or deploy it. Next log imported and render section identities, GUIDs and mapping counts before/after rebuild, then trace the active rebuild path. `LODUtilities.cpp` has a backup/restore path that rebinds clothing, but its involvement here is not proven.

After both failures, `sha256sum -c work/eve26/collision-protected.sha256` passes for shared skeleton, production body physics and the private Holiday mesh. No asset rollback was needed. No cook, installation or release change occurred. The Commander White session handoff is available at `CSS-Mod-Authoring/eins0fx-collections/CSS_CommanderWhite_eins0fx_P/AGENT-PROMPT.md`; its agent should consult this evolving checkpoint rather than copy the unfinished cloth experiment as a verified method.


### Cloth rebuild diagnosis and primary-section repair

`cloth-bind3.log` records imported section index, original index, material slot, cloth asset index/GUID, mapping count and render lookup before and after rebuild. Section indices stay stable. The attempted shared bind restores decal section 16 first, while sections 18-20 lose their GUIDs and mappings. Unreal logs repeated `LOD0 is already mapped to mesh LOD0` warnings. Installed UE 5.6.1 `MeshBuilder/Private/SkeletalMeshBuilder.cpp:706` unbinds clothing before geometry rebuild, `SkeletalMeshUtilitiesCommon/Private/LODUtilities.cpp:3539` restores each section via `BindToSkeletalMesh`, and `ClothingSystemRuntimeCommon/Private/ClothingAsset.cpp:201` rejects reusing the mapped cloth LOD. Do not keep retrying manual multi-section GUID assignment as a durable solution.

The diagnostic also exposed a separate primary-binding bug: section 17 had already lost its imported mapping before the explicit rebuild. `BindToSkeletalMesh` internally uses `FScopedSkeletalMeshPostEditChange`, causing a rebuild before the caller persisted UserSectionsData. Fixed by wrapping binding and metadata assignment in an outer scope so the metadata exists before rebuilding, then finishing compilation.

Built successfully (`cloth-build4.log`). A primary-only `-NoSave` diagnostic (`cloth-primary.log`) retains asset index 0, matching GUID/render asset and 68,507 mappings across another rebuild; fixed/movable particle checks pass. This does not validate multi-material clothing, saved/cooked persistence, collision coverage or runtime motion. The command's new NoSave option returns before MarkPackageDirty/save. All protected asset hashes must remain unchanged for this diagnostic.

Next investigate a supported coherent multi-material route. Epic's Panel Cloth tutorial explicitly describes multiple materials driven by one simulation mesh (https://dev.epicgames.com/community/learning/tutorials/pv7x/unreal-engine-cloth-panel-editor), while the legacy clothing tool works per material section (https://dev.epicgames.com/documentation/unreal-engine/clothing-tool-in-unreal-engine---properties-reference). A panel Cloth Asset requires proving the shipped game's runtime support; do not assume editor plugin availability implies game support. An alternative to evaluate is consolidating garment materials for one legacy cloth section while preserving texture appearance and independent customization masks. Inspect actual material parents, blend modes, textures and UVs before selecting that route. Do not silently drop trim physics, duplicate the full simulation per material, or patch engine binaries to hide the rebuild failure.


### Material audit and panel-cloth compatibility evidence

Read-only `audit_holiday_materials.py` completed in UE (`cloth-materials.log`, `cloth-materials.json`). All five saved garment MICs currently point to WorldGridMaterial and have no direct scalar/vector/texture overrides. These are editor placeholders, not evidence that the installed outfit has no textures. Gemini's `stage_all_variant_materials.py` separately stages compiled material instances and authentic textures for XM_Dress01/02/03, XM_Decal and XM_Hair. Preserve that distinction when rendering or packaging. Its Dress01/02 reuse the Christmas01 base/normal/ORM; Dress03 uses a separate normal; decal and fur use separate texture sets. Do not consolidate based only on the placeholder parent.

The game's existing `Binaries/Win64/ue4ss/CXXHeaderDump/ChaosClothAssetEngine.hpp` (mtime 2026-09-13) includes UChaosClothAsset, UChaosClothComponent, SetClothAsset/SetAsset, pose-parent behavior, collision sources and simulation controls. The current installed shipping executable independently contains the script package and component/asset/method strings; `work/eve26/panel-runtime.json` records offsets, executable SHA256 and scope. This is stronger compatibility evidence than editor availability, but does not prove a newly cooked cloth asset loads or simulates correctly.

Prefer an isolated panel-cloth feasibility candidate next: this supported architecture can preserve five render materials while using one simulation surface. UE 5.6.1 has the installed `Engine/Plugins/ChaosClothAsset` and `ChaosClothAssetEditor` plugins. `UChaosClothAsset::Build(collections)` is the nondeprecated entry point. Collection facades expose sim pattern initialization, bone weights, render patterns, MaxDistance weight maps, skeletal mesh and physics paths. The existing skeletal import node demonstrates render section import, sim morph targets and path assignment; its ClothDataflowTools helper is private, so do not assume public linkage. Current CSS native sources contain no ChaosClothComponent integration. Keep the authoring experiment isolated first, then coordinate runtime component lifecycle, outfit visibility, morphs, preview and customization support with the core owner. No plugin/dependency/project changes have been made yet.


### Saved multi-material panel-cloth candidate

Added `CSSEvePanelCommandlet` and enabled the ChaosClothAsset plugin plus ChaosClothAsset/ChaosClothAssetEngine dependencies in the shared authoring project. Only additive project configuration and new commandlet sources were staged; CSS native game files were not edited. Before files: `panel-project-before.json`, `panel-module-before.cs`. Initial build failed on a guessed GeometryCollectionCore module; installed 5.6.1 places ManagedArrayCollection in the existing Chaos module. Removed that dependency, then build succeeded (`panel-build2.log`). Inspector extension also builds (`panel-build3.log`).

Created `/Game/CSS/EveTest/CA_Holiday` (~13 MB) with one connected 4,336-particle / 8,328-face simulation and five separate render patterns. All geometry is copied from the prepared proxy and the private mesh sections, not refitted. Source normals, tangents, UVs, vertex colors and section bone weights are transferred. The source mesh and production assets are not saved by this command. Render-to-simulation mappings are generated by the supported ClothAsset builder.

Fresh process readback (`panel-saved.json`, `panel-inspect.log`) retains one valid simulation, five exact material references and full one-per-render-vertex mapping counts: decal 1,219; main dress 68,507; fur 18,658; emissive 1,615; extra dress 1,990. These counts reflect the imported mesh's split render vertices; they are not simulation particle counts. The numerical check receipt `panel-verified.json` passes topology counts, material set, mapping counts, anchor counts and the private PA_Holiday reference. There are 3,070 exact-zero particles and 3,116 below 0.1 cm. Protected skeleton, body physics and private mesh hashes still match.

This is a structural feasibility result, not cloth acceptance. The saved candidate currently references SKEL_Holiday_Import, inherited from the source private mesh, and has zero sim morph targets. Resolve the shared-skeleton assignment without changing mesh reference transforms, and transfer/validate supported morph data and propagation before treating this as a usable outfit. The 2D positions are a projection used with legacy isotropic 3D configuration, not a tailored/anisotropic pattern. Clothing collision coverage gaps, material placeholder vs staged texture appearance, simulation stability, CSS component lifecycle/customization, cooking and game playback still require work. The game has not been modified or restarted.


### Combined-morph limitation and existing skirt-bone trial

Before importing panel morphs, checked the actual UE 5.6.1 runtime. `ChaosClothingSimulationCloth.cpp:87-88,182-188,902-908` stores one ActiveMorphTarget index and weight per LOD; SimulationMorphTargetConfigNode likewise exposes one name/weight. ClothAssetBase initializes rendering without ordinary mesh morph targets. Adding six sim morphs to the candidate would therefore not establish Eve's six independently combinable body sliders. Do not claim full customization from single-morph endpoint tests or ship a restriction to one slider. The panel asset remains a useful structural prototype, but no more panel changes were made in this turn. A future combined-morph adapter would need separate proof.

Investigated the existing bone-driven route because it retains the normal skeletal-mesh morph pipeline and all material sections. The current skeleton already has four CSS_Cloth_Skirt chains, each six bones. Component-space audit `skirt-bones.log` puts the first three joints at approximately z=108/100/92 cm, spanning Holiday's lower skirt (hem about94 cm). No new bones or rest-pose changes are needed for a trial. `CSSDynamicsRecipe.inl` in the authoring source already supports native AnimDynamics chains and collision spheres. Current CSS core has no literal skirt-chain integration; the route must extend a private copy of the accepted secondary graph without replacing body/idle behavior.

`prepare_skirt_bones.py` creates `holiday-bones.mesh.json`, changing only 4,963 of the five dress material regions' 16,496 point weights, plus its private mesh package name. Lower skirt weights blend continuously around four directions and between the first three existing joints, with a smooth transition below117.8838 cm. It obeys the established eight-influence export cap; any discarded mass must be below1.5%. `skirt-bones.json` hashes every protected export field: points, faces, normals, UVs, skeleton and all22 morphs are unchanged. Body weights outside the selected garment points are preserved. This is an export trial, not an edit to the original blend or a runtime physics result.

`render_skirt_bone_trial.py` applied a synthetic outward four-degree bend per joint, first three joints per chain, to the whole rendered garment. Rendered default and combined PBMHipSize=1/PBMWaistWidth=1 front/side views in `skirt-bone-views`; reviewed default front and combined front/side against the earlier `section-views/front.png`. Hem/trim remain together in these views. A gray hip patch persists on the viewer-left front side and is also present in the baseline; investigate its material/surface identity and repair local garment clearance rather than changing the body. Sleeve/chest refinements remain visible. These are synthetic skinning checks, not simulated cloth, real gameplay poses or collision acceptance.

Next: measure and repair that baseline hip patch, apply the verified weight trial to a separate source-blend candidate if suitable, and build a private secondary graph with skirt dynamics while preserving accepted body/hair/idle logic. Test collisions, multi-slider combinations and actual gameplay movement before selecting this route for release. Do not revive manual multi-section legacy cloth bindings or present the panel prototype as finished clothing.


### Holiday hip overlap source repair, validation pending

A local front hip patch is undergarment material `MI_CH_P_EVE_Christmas_01_01.005` showing through outer dress `.001`, not exposed body. `hip-patch-surfaces.json` identifies 36 default and 53 hip-plus-waist grid samples. Grazing ray depths exaggerate clearance: nearest-surface measurements are at most 0.119 cm default and 0.171 cm with hip/waist enlarged. Do not apply the roughly 1.7 cm ray depth as an outward offset.

`prepare_hip_clearance.py` makes a geodesically tapered correction to 76 outer-dress vertices, at most 0.25 cm outward. Nine local grid checks (default, each of six morphs, hip plus waist, all six) show no visible undergarment in that region. Front renders of default and combined hip/waist confirm the patch is gone. This is local evidence, not full outfit or gameplay acceptance.

`apply_hip_clearance.py` saved separate `work/eve26/holiday-f11.blend`. It reproduces the fitted export vertex correspondence exactly, snapshots every shape before editing and adds the same displacement to all relative keys. The original body digest is unchanged. Fresh export `holiday-hip-source.mesh.json` matches the geometry trial within 0.00000914 cm, preserves body points exactly, and preserves all 22 relative morphs within 0.00000144 cm.

The verifier intentionally still fails on skin influences: 14 vertex/bone weights across two vertices differ, maximum absolute difference 0.00006448. These vertices are outside the 76-point correction. Do not loosen the comparison or accept the fresh export until investigated. A four-group cleanup reproduction did not fail, but running the existing exporter cleanup on actual F10 garment data left a sub-threshold group behind. Direct saved-source comparisons and a snapshotted cleanup audit are next. No exporter fix is yet established by this checkpoint. Original F10, production assets and installed release remain unchanged.


Weight diagnosis completed: `source-weights.json` proves all four garments have identical raw weight hashes in saved F10 and F11, while the old cleanup produces different Dress hashes. `clean_weights.py` snapshots integer group IDs and weights before removal, checks that removal emptied the vertex groups, then restores retained normalized influences. `check_source_weights.py` passes on both blends (`source-weights-stable.json`, exit zero): identical cleaned hashes for every garment and no sub-threshold leftovers. The small synthetic four-group test was insufficient; the actual garment exposed the fault. Gemini's reference exporter remains unchanged. The Eve export wrapper now explicitly uses the tested helper and records its hash.

Next export both F10 and F11 through this corrected wrapper to separate unused private files, then compare weights exactly and geometry/morphs against the accepted local repair. The previous `holiday-hip-source.mesh.json` still came from the old cleanup and is not the corrected deliverable. The strict verifier remains failing against that old output by design. Combine the verified F11 source with the existing skirt-bone weighting trial only after the fresh exports pass. Then evaluate real cloth motion while preserving current hair/body physics and independent body morphs. No game or release changes in this step.


### First combined Holiday motion candidate, rejected

Added private `CSSEveMotionCommandlet` and staged it into the authoring module. It duplicates `/Game/CSS/SeduXtress/ABP_Secondary` into unused `/Game/CSS/EveTest/ABP_Holiday`, appends four existing AnimDynamics chains after the existing output, and checks original node GUID/classes and CSS default values. Both builds succeed (`motion-build.log`, `motion-build2.log`). The saved graph retains 34 original nodes and adds twelve controlled skirt bones (`motion-create.log`). Source graph, shared skeleton and gameplay body physics hashes remain unchanged (`motion-protected.json`).

`holiday-dynamics.json` is an initial trial, not tuned clothing. Four three-body chains use midpoint offsets from existing skirt bones, 4/1/1 boxes, 0.5 cm body spheres, damping 0.9, spring 100, gravity 0.1 and component-motion scale 0.1. Each takes the nearest sixteen previously fitted collision spheres. Collision coverage remains approximate. No rest-length post-correction is enabled. Epic's [AnimDynamics documentation](https://dev.epicgames.com/documentation/unreal-engine/animation-blueprint-animdynamics-in-unreal-engine) describes the pivot/box distinction; installed 5.6.1 `AnimNode_AnimDynamics.cpp:736` establishes the offset sign used here.

Imported `holiday-bones-import.json` into private `SK_HolidayBones` and `SKEL_HolidayBones`: 67,613 points, 119,534 triangles, 379 authoring bones and 22 morphs. Its materials are placeholders. The editor probe binds that private mesh to the current shared skeleton in memory only, after checking every bone and parent. Tracked `native/holiday-probes.patch` records exact-path extensions to the existing binding and compressed-clip evaluators. Source backups end in `.before-holiday` under `work/eve26`; do not restore them over later authoring work. CSS runtime code was not changed.

First Python invocation used `-ExecutePythonScript` outside commandlet mode and correctly failed the binding guard. That process subsequently exited normally. Use `run_ue.py --log <name> -- -run=pythonscript -script=<absolute script>`; the launcher now rejects non-commandlet invocations. The correct second run (`holiday-motion-eval2.log`) produced 145 walk, 81 jog and 65 sprint frames, from compressed clips and actual component evaluation. It preserves unrelated bones within approximately 1.9e-14 cm / 6e-8 radians, but this is NOT motion acceptance.

`verify_holiday_motion.py` rejects all three recordings. Local skirt translations grow to roughly 8.8e12, 3.8e13 and 5.1e13 cm; the back chain's third bone turns almost 180 degrees at frame one. Finite matrices and untouched other bones are insufficient. Do not render these enormous poses, deploy the graph or use its initial recipe as a standard. `holiday-motion-verdict.json` records rejection. The evaluator now adds a coarse displacement guard after saving diagnostic data. `render_skirt_bone_trial.py` supports measured snapshots, but no simulated render was accepted in this step.

Next isolate the angular spring and collision contributions in a controlled test. First read `CSS-Mod-Authoring/docs/next-gen-dynamics-investigation.md`, especially V31-V35: the non-root spring target is in a different space, serial independent bodies were rejected, and inertia changes did not establish a general solution. The initial Holiday recipe omitted non-root target correction. That is a known setup concern, not yet proof of the entire divergence's cause. Do not repeat the old long sequence of inertia experiments or hide the explosion with rest-length post-correction. The existing accepted ControlRig chain approach remains available if the built-in solver requires further architectural repairs.

### Holiday motion controls and second candidate (2026-09-26)

The preceding next-step paragraph is superseded by these completed controls. `motion-controls.json` tests spring off, contact off and both off on the first graph. All still diverge to approximately 9e12 cm. `motion-inertia.json` separates reset-only from equal-axis inertia: reset alone still diverges, while cubes reduce maximum displacement to 3.74 cm; cubes without spring/contact reduce it to 0.88 cm. This matches the already documented unequal-axis initialization problem in `CSS-Mod-Authoring/docs/next-gen-dynamics-investigation.md`. No engine patch was made.

`motion-cube.json` then isolates the residual: spring-off/contact-on displaces walk/jog/sprint by 3.76/5.30/5.09 cm; contact-off walk stays below 0.90 cm. Sprint reaches 67.9 degrees and the measured renders in `sprint-cube-views` show flare and clipping. `skirt-rest-contacts.json` measures up to 3.18 cm initial overlap between simulated centers and body spheres. These are diagnostics, not accepted settings.

`fit_skirt_body_centers.py` derives centers from weighted main-dress geometry, projects them onto the fitted surface, and moves them outward 0.35 cm. It preserves mesh/body geometry, weights and the skeleton. Recipe `holiday-dynamics2.json` uses equal-axis boxes, 0.25 cm collision radius and no angular spring. All twelve reference-pose centers clear the existing spheres, by 1.98 to 7.32 cm. Rest clearance alone does not prove animated contact.

`centers-build-console.log` confirms the editor helper build succeeded. `centers-create.log` creates unused `/Game/CSS/EveTest/ABP_Holiday2` successfully. `holiday2-motion.log` then evaluates actual compressed walk and jog clips for two cycles. The script rejects jog and exits 255; sprint is deliberately not reached. `holiday2-motion-verdict.json` independently reports walk 6.79 cm / 42.47 degrees, jog 18.30 cm / 58.16 degrees, and missing sprint. Passing the coarse walk guard is not garment acceptance. `jog-centers-views/default-side.png`, inspected after successful Blender rendering, confirms hip exposure, displaced hem and garment distortion at jog frame 6. This graph must not be deployed.

Editor probe changes are recorded in `holiday-controls.patch`, `holiday-inertia.patch` and `holiday-centers.patch`, following the previous `holiday-probes.patch`. The optional diagnostic bitmask is restricted to the private Holiday assets: 1 disables spring, 2 disables spheres, 4 uses equal-axis inertia and 8 resets dynamics. These controls modify transient instances only. All current processes exited; no game install, restart, cook or release replacement occurred.

Next:

1. Adapt the existing ControlRig solver's fixed-step scheduling, length/cone constraints and reset behavior to the four skirt chains. Consult `build_controlrig_full_hair.py` and `controlrig_full_hair_contacts.py`, plus the V10 evidence in `next-gen-controlrig-full-hair.md`. Do not execute the old hair builder unchanged: it hardcodes 36 points, 379 imported bones and the obsolete `/Game/CSSAuthoring/` namespace.
2. Derive cloth-specific anchors, limits and collision coverage from F11 and the current twelve weighted skirt bones. Preserve the current shared skeleton and existing secondary graph. Prove combined morph support and inspect actual skinned poses, not just solver points.
3. Complete whole-outfit assembly and game checks only after the isolated motion candidate passes. The body proportions and fitted source remain untouched.

### Preserve fitted animation before adding secondary motion (2026-09-26)

The first two steps above were refined by a same-pose comparison. `jog-fit-upstream` and `jog-bones-upstream` use identical recorded jog frame 6 before secondary dynamics. The fitted F11 weights cover the thigh; the experimental circumferential skirt weights cut through it. Both side renders were inspected. The old skirt controls inherit pelvis motion, so the failed solver was also being asked to reconstruct missing leg-follow deformation. This is a separate regression from the solver instability.

`measure_skirt_drivers.py` tested averaging original deformation drivers for each experimental skirt bone. Error against fitted skinning fell from 15.91 to 6.24 cm maximum, but `jog-driver-views/default-side.png` still shows a skin patch. Retain this as a rejected approximation, not a production driver recipe. Reports identify generated transforms separately from actual measured component poses.

`prepare_skirt_follow.py` instead maps eleven original garment drivers one-to-one onto eleven existing skirt control bones. It changes only bone indices on 41,954 garment influences. It preserves every influence value and count, all non-garment influences, mesh geometry, materials, UVs, 22 morphs and the skeleton. `holiday-follow.mesh.json` remains an offline private export. These bones act as independent control carriers, not a newly reparented skeleton or the previous four-chain solver. The rest of the garment's original drivers remain untouched.

For each carrier, its required component transform is `driver_current * inverse(driver_bind) * carrier_bind` in column-matrix notation. This preserves the original skinning deformation exactly before dynamics. `verify_skirt_follow.py` serializes through local translation/rotation/scale and compares all mesh points against original fitted skinning. Default and combined hip/waist at jog frame 6 differ by at most 0.000064 cm. `jog-follow-views/default-side.png` was inspected and restores the fitted coverage. These are offline checks, not a new game acceptance.

`build_skirt_follow.py` creates `/Game/CSS/EveTest/CR_HolidayFollow`, a 67-node rig using native GetTransform, MakeRelative, MakeAbsolute and SetTransform units. It imports the private 379-bone mesh hierarchy only; it neither replaces nor edits the improved shared skeleton. Initial driver/carrier relative transforms supply bind compensation. Child propagation is disabled so unrelated bones retain their component transforms. The multiplication order is checked against installed 5.6.1 `RigVMFunction_MathTransform.cpp`; Epic's [Set Global Transform reference](https://dev.epicgames.com/documentation/unreal-engine/BlueprintAPI/URigHierarchy/SetGlobalTransform) explains preserving children when propagation is disabled.

Creation completes successfully (`follow-build.log`). The first direct evaluator failed before execution because RigElementKey positional arguments were reversed; the corrected script uses named fields. A fresh process reload (`follow-eval2.log`) executes the saved rig on 145 walk, 81 jog and 65 sprint upstream poses from the recorded compressed-clip tests. Four affinely independent points compare each carrier's skinning transform with its original driver. Maximum error is 0.0000029 cm; unrelated component-transform error is zero. `skirt-follow-evaluated.json` records this limited scope. Shared skeleton, existing secondary graph and gameplay body physics hashes still match.

Next integrate this base-follow rig into an unused copy of the current secondary graph and import the paired private mesh. Check real component execution before adding bounded secondary motion. Keep torso/neckline carriers anchored to their original drivers; derive lower-garment pivots, motion limits and collision coverage from the fitted cloth. Reuse the existing fixed-step/reset discipline where applicable, but do not copy the hair solver's fixed point count, ponytail/head assumptions or collision margins. Combined morph contact, attacks, full outfit assembly, game verification and all palettes remain required. No game or release files changed in this step.

### Carrier integration passes actual component evaluation (2026-09-26)

The preceding import/integration step is complete. `CSSEveMotionCommandlet -Follow` creates only unused `/Game/CSS/EveTest/ABP_HolidayFollow`, appending the saved carrier rig to a copy of the current secondary graph. It preserves all 34 original node identities/classes and CSS control types/defaults. The output filter includes all 24 existing skirt bones so compensating child-local transforms survive pose transfer, while only eleven carriers are driven. The rig receives the incoming pose and curves, resets its input hierarchy and uses local-space pose transfer. No engine or game runtime code was edited.

`holiday-follow.patch` records the exact private mesh/graph extensions in CSSRetargetLibrary and CSSAnimationLibrary. The evaluator also permits the original ABP_Secondary on the same private mesh for a controlled comparison. Old AnimDynamics paths retain their guards. Source backups are `.before-follow` in `work/eve26`; preserve later changes rather than restoring these blindly. `follow-module-console.log` confirms the editor module build succeeded. Commander White was using a separate cw7 authoring project, which was left alone.

Imported the paired mesh with `holiday-follow-import.json`, using its own `SKEL_HolidayFollow` package. It is bound to the shared skeleton only in memory during evaluation, after bone/parent compatibility checks. `follow-import.log` and `follow-graph.log` complete successfully. The import remains the private partial outfit with placeholder materials.

`evaluate_holiday_follow.py` evaluates the original graph and the copied graph on the same mesh, separately, for two cycles of each actual compressed animation. `follow-component.log` exits zero; `follow-component.json` reports exact non-skirt local-pose equality for 145 walk, 81 jog and 65 sprint frames. Six raw pose files are named `follow-{walk,jog,sprint}-{base,candidate}.json`. Existing hair/body/hand output is therefore preserved in this sampled comparison.

`verify_follow_component.py` reconstructs component-space transforms independently and checks four affinely independent points per carrier against its original driver after the full secondary graph. `follow-skinning-verified.json` passes: maximum errors are 0.000381/0.000576/0.000571 cm for walk/jog/sprint. This checks the carrier skinning mapping, not contact or all gameplay poses. `follow-component-views/default-side.png` and `hip-waist-side.png` were inspected from actual candidate jog frame 6. They retain fitted thigh coverage. The morph view changes geometry on that recorded pose; it does not resimulate contact for the changed body shape.

Next derive and implement bounded lower-garment secondary motion on this baseline, preserving anchored torso/neckline carriers and the existing body motion. The accepted body solver's `build_controlrig_body_timed.py` and `controlrig_body_step.py` also provide fixed-step angular dynamics, reset handling and limits; inspect them before implementing another solver. Their seven-helper layout and anatomy-specific contact assumptions cannot be copied unchanged. Measure pivots/garment support and collision against actual skinned cloth, including combined morphs. Keep the accepted carrier baseline separate for future comparisons. No game restart, installation, cook or release replacement occurred; all processes from this step have exited.


### Broader contact sweep and hem controls (2026-09-26)

`measure_follow_clearance.py` samples actual original-graph walk/jog/sprint poses on F11, including default and combined hip/waist geometry. `follow-clearance.json` covers 62 cases and 3,642 lower main-dress vertices per case. Sprint frame 48 reaches 4.30 cm default and 4.31 cm combined penetration. Six-direction odd ray crossings in `follow-sprint48-rays.json` confirm body containment for the deepest rear-hem samples. Their original weights are predominantly pelvis while the intersecting body surface follows the left thigh. Preserving original deformation alone therefore does not establish good fitting throughout motion.

`prepare_hem_controls.py` splits lower pelvis weights into four regional controls (15 total with the previous carriers). `prepare_hem_regions.py` preserves original driver mass while assigning lower garment regions to 22 existing skirt bones. It modifies 2,815 garment vertices and keeps at most eight influences. Consolidation merges only copies of the same original driver, preserving baseline skinning but approximating regional blending. Neither trial edits body geometry, morph deltas or skeleton transforms. `sprint48-hem-verified.json` and `sprint48-hem2-verified.json` confirm baseline point errors below 0.000054 cm for default and combined hip/waist. These are offline exports, not UE imports or new runtime graphs.

`probe_hem_projection.py` is a bounded static contact feasibility test on sprint frame 48, not a cloth solver. The four shared translation controls on hem2 leave 1.64 cm penetration; outward-only radial projection leaves 4.14 cm. Reject these as fitting solutions. Independent translations on all 22 carriers reduce the minimum to -0.863 cm with 20 vertices deeper than 1 mm and maximum garment movement 6.37 cm. Some carriers reach the 8 cm diagnostic cap. Do not deploy these corrections: temporal continuity, shape preservation and other poses have not been checked. Receipts are `hem-contact.json`, `hem2-contact.json`, `hem2-radial.json` and `hem2-independent.json`.

Reviewed side/rear diagnostic renders in `follow-sprint48-views`, `follow-sprint48-rear`, `hem-contact-views` and the independent trial's `hem2-independent-views/default-side.png`. The latter still shows a skin patch near the upper hip. The gold rear panel in these flat-color renders is an authored decal shown opaque, not evidence of underwear clipping. Diagnostic colors are not final materials.

The upper-hip patch initially looked like a separate anchored fitting defect. `hip-sprint48.json` corrects that interpretation: all eight worst upper-region samples in each morph case intersect triangles weighted entirely to `lowerarm_l`. Six-direction rays support containment. With arm triangles excluded, `hip-sprint48-noarms.json` measures minimum -0.0152 cm default and -0.0273 cm combined, with zero samples deeper than 1 mm. This exclusion isolates the cause; it does not waive arm clipping from final acceptance. Do not transfer forearm weights onto the hip or enlarge the dress to hide it.

Next separate lower-hem/thigh collision work from the swinging-arm contact. The hem needs bounded, temporally coherent control while preserving garment shape, followed by the same actual clip and morph checks. The arm contact needs a same-pose comparison against the body and sleeve before choosing garment collision or a narrowly scoped outfit adjustment. Preserve accepted gameplay animations and the body. The private native `-Follow` commandlet still expects the eleven-carrier recipe; it does not support hem2 automatically. No game restart, install, cook or release archive replacement occurred. All processes for these checks exited successfully.


### Persistent hem projection fails motion and shape checks (2026-09-26)

`probe_hem_sequence.py` applies the 22-carrier projection across all 65 recorded sprint frames (two cycles, 60 Hz). It carries prior corrections in the pelvis rotation frame, decays them over 80 ms, projects contacts for twelve iterations and caps each carrier at 8 cm. This is a diagnostic contact sequence, not a physical cloth solver: it has neither fabric constraints nor inertial integration. The test uses the original accepted body/animation output and does not edit either.

`hemseq2.json` rejects this method as a motion candidate. Maximum correction change between adjacent frames is 8.392 cm at frame 11. Maximum fabric-edge length relative to the uncorrected same-pose edge is 13.323 at frame 17 (edges shorter than 0.01 cm excluded). Maximum cloth displacement is 7.9996 cm at frame 38, which still has 176 controlled vertices deeper than 1 mm. Worst remaining signed penetration is 2.653 cm. These metrics include whole-body contact, so anchored arm contact remains in the overall count; the controlled count separately requires more than 0.1 summed carrier weight. Edge ratios compare against the animated baseline, not the reference-pose fabric length.

The first run completed calculations and pose serialization but failed writing its report because a NumPy integer was not JSON serializable. The count now converts explicitly to Python int. A fresh run completed successfully; `hemseq-pose.json` and `hemseq2-pose.json` compare equal. The frame-24 rear render in `hemseq24-views/default-rear.png` was inspected and shows a stepped, distorted hem. Its gold patch is still the opaque diagnostic decal. Combined-morph renders in that directory reuse the default-body solved pose; they are not a combined-morph contact result.

Do not continue tuning this unconstrained projection or deploy it. The next lower-hem experiment must constrain fabric deformation as well as body contact and measure temporal changes. Use the existing accepted baseline for comparison. Any constrained control formulation must show that the available carrier weights can satisfy both contact and shape limits; if they cannot, revise garment control weights or use the existing panel-cloth reference rather than increasing the displacement cap. The separate upper-dress/forearm overlap still needs a sleeve/body same-pose diagnosis. Source blends, current shared skeleton, accepted gameplay animations, installed mods and releases remain untouched. Both Blender sessions are terminal.


### Joint edge/contact carrier test and refreshed fabric surface (2026-09-26)

Checked Matthias Müller's [cloth constraint notes](https://matthias-research.github.io/pages/tenMinutePhysics/14-cloth.pdf), which describe edge-distance constraints and separate bending resistance. This supports measuring fabric deformation alongside collision, not treating contact projection alone as cloth simulation. The following reduced carrier-space test is our diagnostic least-squares formulation, not an implementation of XPBD from that reference.

`probe_hem_sequence.py --shape --end 17` jointly minimizes relative edge-length residuals and contact residuals over 66 carrier translation variables, with a temporal prior. Step size is bounded to 1 cm per iteration before 0.8 relaxation, and the prior 8 cm total cap remains. `hemshape.json` covers 18 consecutive sprint frames. Against the same first 18 unconstrained frames, maximum edge ratio falls from 13.323 to 3.606 and maximum correction change falls from 8.392 to 3.327 cm, but maximum controlled penetrations deeper than 1 mm rises from 175 to 239. Soft constraints do not guarantee edge limits; this result remains unacceptable.

`hemshape8-views/default-side.png` was visually inspected and shows thigh clipping through the lower dress and distorted trim. Combined-morph images again reuse the solved default pose and do not validate contact for that morph. Do not deploy or keep adjusting arbitrary penalty weights without first checking the deformation space itself. The test does not prove no carrier solution exists, only that this formulation does not satisfy the requirements.

Refreshed the existing connected fabric surface from the corrected F11 export with `extract_skirt_proxy.py --mesh work/eve26/holiday-hip-clean.mesh.json --name skirt-f11`. `skirt-f11.json` retains exact source vertex IDs, current positions, normals and normalized weights. Topology stays 4,336 vertices, 8,328 triangles, 346 boundary edges, zero nonmanifold edges. This supersedes the older surface's F10-era geometry/weights for new fitting experiments; existing panel assets are not silently rebuilt.

Next test direct fabric-surface constraints with anchored upper dress and body collision on the failing poses. This is a reference to establish whether cloth geometry can maintain shape and coverage before fitting its motion into limited bone controls. Include rest/morph-dependent edge lengths, bending and temporal checks as that reference develops; no single static result suffices. The refreshed source ID mapping allows measured positions to be rendered against the full outfit without changing the original body or skeleton. A production path still needs exact material attachments, combined morph support, runtime integration and game validation. All processes in this step exited; no installed or release assets changed.


### Direct fabric reference exposes surface-contact gaps (2026-09-26)

`probe_fabric_surface.py` uses the refreshed 4,336-point F11 panel directly, without carrier reduction. Existing original-graph sprint snapshots supply moving body geometry and pinned upper-dress targets. Four substeps per recorded frame solve hard edge-distance constraints, compliant opposite-vertex bending and body contact for free vertices. Inverse mass fades from full below z103 to zero at z112. Rest lengths use the selected body-morph geometry, rather than the already deformed same-pose edges. This is an offline reference, not a production cloth implementation: self collision, continuous collision, friction, trim attachments and runtime integration are missing.

`fabric1.json` is an 18-frame initial trial. Its initialization incorrectly converted collision-settling displacement into velocity. Fixed initialization now settles against the first pose with zero velocity before starting the animation. `fabric2.json` completes 18 frames with that correction; frame zero speed is exactly zero. Free-vertex contact is substantially improved, but edge stretching, collapse and excessive motion remain. At frame 16, maximum displacement from skinned fabric is 23.99 cm and maximum edge length/rest length is 4.131. This is not an accepted candidate. Initial-settling improvements do not justify further gameplay claims.

`render_skirt_bone_trial.py --surface-motion` can display measured fabric point positions on the full mesh. It checks source motion identity/frame and renders only the matching morph case, preventing an untested morph from appearing as a solved result. `fabric1-views/default-side.png` was inspected: the main dress still has a visible body patch, and fur/decals do not follow the independent surface. Those attachments are deliberately not simulated yet. The renderer preserves the body and other garment positions.

Added barycentric surface checks after the visual patch contradicted the free-vertex clearance result. `fabric3.json` reproduces the corrected solver through frame 8, adding triangle-centroid and three edge-midpoint probes on faces with any free vertex. At frame 8, free vertices have no penetration deeper than 1 mm, but 168 surface samples exceed 1 mm, with the deepest at 1.079 cm. These are sample counts, including shared-edge duplicates, not unique faces. Face/edge checks are required in future clearance gates; vertex-only clearance is insufficient. No render or solver result here is release-ready.

Next incorporate surface contact constraints and stable cloth initialization/temporal behavior into the reference, then check visible shape, cloth attachments and combined morphs. Do not fit the failed reference motion to bones or deploy it. Distinguish penetrations in anchored arm-contact regions from lower-hem/leg contact, and retain the accepted body and animations. A native/runtime implementation must use a proven cloth path rather than copying this slow Python diagnostic. Source blends, shared skeleton, installed mods and release archives remain unchanged. All processes in this step exited successfully.


### Surface constraints, substeps and anchoring audit (2026-09-26)

`probe_fabric_surface.py --surface-contact` adds inverse-mass-weighted barycentric contact corrections for face centres and unique edge midpoints. The receipt records substeps, iterations and pin heights. Contact diagnostics now label worst samples as movable or fixed. This addresses samples between vertices but is not continuous triangle collision or proof of complete surface coverage.

Compared 4 substeps x 8 iterations (`fabricface.json`) with 16 x 2 (`fabricsmall.json`), each through sprint frame 8. Both have 32 constraint sweeps per moving frame, but rebuilding the body BVH more often adds work; this is not an equal-runtime performance comparison. The rationale is [Small Steps in Physics Simulation](https://matthias-research.github.io/pages/publications/smallsteps.pdf), which compares smaller XPBD steps against more iterations. Our results do not establish that this particular implementation is stable: maximum edge ratio is 8.542 vs 10.522, and peak speed is 795 vs 2,311 cm/s. Surface sample failures fall relative to vertex-only collision but remain. Do not deploy either trial.

The two deepest persistent frame-8 samples are on pinned edges at rest z112.38–112.44 cm. Source vertices 48041/48037 and 48037/48034 cannot move under the previous z112 pin cutoff. Nearby free fabric stretches around them. The worst short edge in the small-step trial joins source vertices 51319/51320, about 2.07 mm long at rest near z111. This identifies an anchoring conflict, not justification for editing Eve's body or accepted animation.

`fabricwaist.json` tests pinning above z120 and full free mass below z110, retaining 16 substeps x 2 iterations and surface contact. Although frame 8 has no sampled penetrations deeper than 1 mm, the full nine-frame window reaches edge ratio 15.911 and speed 7,599 cm/s. The frame-8 side render `fabricwaist8-views/default-side.png` shows folding and separation from the unsimulated trim. No acceptance: selected-frame contact improvement hides transient instability and missing attachment behavior.

`probe_body_step.py` / `bodytrace.json` in the work directory measure original body and dress target motion independently of cloth. Maximum whole-body vertex displacement between these recorded frames is 14.66–26.22 cm, while main-dress target displacement is 1.93–6.63 cm. Whole-body maxima may involve distant limbs and are not lower-skirt collider speed; classify actual contacted triangles before drawing a conclusion about the source animation. Do not change accepted animation on this evidence.

Next audit contact trajectories and corrections on the unstable frames, with pinned-contact conflicts separated from free fabric and attachment gaps. A production candidate requires a stable contact method, bounded stretch across the complete sequence, attached fur/decals, combined morph checks and game validation. Increasing solver counts or moving the pin boundary alone has not solved the problem. The offline trials remain private; no game files, source blends or releases changed. Concurrent edits in `native/src/engine.hpp` and `native/src/walk_override.inl` belong to the CSS agent and were left unstaged.


### Arm contact drives the largest spike; attachment transfer remains diagnostic (2026-09-26)

Added bounded contact traces to the direct fabric reference: each frame retains its eight largest corrections with source cloth IDs, inverse masses, body triangle vertices, normal and substep. `fabrictrace.json` reproduces the waist-pinned trial. `fabrictrace-drivers.json` resolves the largest corrections against original body weights. At frame 3, a surface correction moves one vertex 5.576 cm in one substep. The cloth triangle uses source vertices 49711/49710/49709, with inverse masses 0.03327/0.001153/0.000335. Its collider vertices 6611/6648/6647 are predominantly `upperarm_r` (94–97 percent). Frame 2 has a similar 4.757 cm correction against the right upper arm. The large velocity spike therefore involves almost-pinned waist fabric and the moving arm, not just the lower hem.

`--contact-region torso-legs` isolates solver collisions by excluding triangles whose vertices are predominantly arm/hand weighted. **Final vertex and surface clearance still uses the whole body**, and the output records both scopes. `fabrictorso.json` reduces maximum speed from 7,599 to 681 cm/s and maximum edge ratio from 15.911 to 2.104 over the same nine frames. Whole-body surface failures increase to 199. This control attributes the instability but is not permission to ship ignored arm collisions. Next inspect whether those arm/waist intersections also exist on the unchanged body itself before deciding between garment contact changes and a narrowly scoped pose correction that preserves hand targets. Body self-intersection is currently a hypothesis, not a finding or authorization to alter body proportions.

Added diagnostic attachment transfer to `render_skirt_bone_trial.py`: `--transfer-trim` barycentrically transfers panel displacement to the other dress sections; `--rotate-trim` additionally uses a proper rotation extracted from each triangle's deformation. Both are isolated rendering options, not exported rig weights or production mappings. They touch only dress material vertices and enforce that no body vertices are mapped. 12,160 attachment vertices are mapped; maximum rest-reference distance is 20.474 cm, so the mapping is not restricted to nearby hem trim.

Inspected `fabrictorso-attached/default-side.png` and `fabrictorso-rotated/default-side.png`. Displacement-only transfer folds/flattens trim; independent triangle rotations produce severe spikes. Reject both as production attachment methods. A coherent supported render-to-simulation mapping with smooth orientation and suitable proxy coverage is still needed. These failures do not establish that the existing saved Chaos panel mapping fails; that asset has not been simulated here. Do not deploy these raw per-triangle mappings.

All contact and render processes exited successfully. Source blends, shared skeleton, game assets and release archives are unchanged. CSS native files remain owned by the other agent and were not staged.


### Traced arm samples are outside the torso; velocity/mass controls do not solve cloth (2026-09-26)

`audit_arm_clearance.py` reconstructs the exact frame-2/substep-15 and frame-3/substep-0 body positions used by the worst traced contacts. It measures the implicated arm vertices against torso-weighted triangles only. `arm-torso.json` reports positive signed separation of 3.45–4.79 cm. Each sampled vertex has ray counts `[0,2,0,0,0,0]`. The torso subset is open at limb/neck boundaries, so this is not a whole-body watertight-volume proof, but these samples do not support the proposed body-self-intersection diagnosis. Preserve the body and accepted poses. Broader arm/torso intersection has not been established or ruled out.

Consulted the primary [Position Based Dynamics paper](https://matthias-research.github.io/pages/publications/posBasedDyn.pdf) on collision velocity handling. `--velocity-contact` is a diagnostic zero-restitution-style velocity correction after position projection. It preserves pre-projection separating velocity relative to the moving collider rather than converting all overlap correction into separation speed. Body velocity uses the mean velocity of the contacted triangle's vertices. This approximation is not a finished collision solver and should not be copied into runtime code as proven physics.

`fabricvel.json` completes the same nine-frame waist-pinned, all-body collision test. Maximum speed drops from 7,599 to 5,195 cm/s and maximum edge ratio from 15.911 to 11.767, but surface failures increase from 11 to 22 and the inspected `fabricvel4-views/default-side.png` still shows severe garment/body overlap and folding. Reject it as a motion candidate.

A separate `--mass uniform` control isolates the inverse-mass gradient near the pinned boundary. `fabricmass.json` changes only free-particle masses from the previous height gradient to equal masses, with the same fixed region, contact and substeps; velocity-contact correction is disabled for this control. It does not solve the instability: peak speed is 8,568 cm/s, maximum edge ratio 26.588, and there are up to 17 failing surface samples. Do not attribute the failure solely to the graded mass or retain uniform mass as a proven fix.

The offline reference still lacks continuous collision and stable coupled contact handling. Further arbitrary mass/damping tuning is not supported by these controls. Trace the moving cloth/body contact crossing or use a validated collision implementation before another production-mapping attempt. Keep the existing Chaos panel asset and single-section legacy-cloth evidence available as engine-supported alternatives, with their documented morph/material limitations still unresolved. No body edits, pose changes, game installation, cook or release replacement occurred. All processes are terminal.


### Native Chaos motion evaluated, fitting still rejected (2026-09-26)

The user clarified that asking why Holiday Reveler takes time was curiosity, not a priority change. Continue fitting Holiday, then all remaining outfits under the existing full customization/physics goal.

Added a read-only `CSSEvePanel -Motion` path using the saved private SK_Holiday and CA_Holiday assets, recorded sprint bone poses, a poseable leader and the actual Chaos cloth component. It waits for simulation completion and exports particles transformed from reference space. No assets are saved. Editor builds completed successfully.

The first run used a zero-time initialization tick and returned bind-pose cloth on frame 0. A 1/60-second reset tick fixes that initialization. `panel-motion2.json` contains nine completed frames. `verify_panel_motion.py` independently reconstructs the exact panel proxy skinning, using its own source weights. All fixed particles match within 0.0000286 cm, confirming pose/coordinate ordering. This is not a fitting pass: maximum tested edge ratio remains 13.476.

`prepare_panel_render.py` requires a passing verification receipt with the exact input SHA256, checks source vertex positions and particle counts, and prepares the main surface for diagnostic rendering. Inspected `panel-motion2-views/default-side.png` at sprint frame 4. There is substantial visible thigh penetration and fabric distortion. Reject this panel/configuration as a production candidate. The render uses the old panel geometry and only replaces its main surface; trim remains skinned, so it does not validate the engine render attachment mapping or F11 fitting.

Both motion commandlets and the Blender verification/render processes exited successfully. `panel-motion2-protected.json` confirms unchanged shared skeleton, production PA_Body, private skeletal mesh and cloth asset hashes. No game install, cook or release changes. Next investigate the native collision coverage and garment constraints on a private candidate, preserve the accepted body/poses, and address supported trim mapping and combined morphs before runtime acceptance.


### Native stretch attribution and iteration control (2026-09-26)

Extended `verify_panel_motion.py` with the eight worst edges per frame, endpoint source weights, rest/actual/skinned positions, movement limits and the unsimulated skinning edge ratio. `panel-motion2-edges.json` confirms that frame 7 edge 2386/2388 grows from 0.834 cm to 13.476 times rest length during simulation, while skinning alone gives ratio 0.99787. Both endpoints have 18 cm MaxDistance and almost entirely pelvis weights. This case is not explained by original skinning stretch or fixed endpoints.

Corrected an investigation hypothesis before changing the asset: projected X/Z pattern coordinates are not established as the source of shrinkage. In UE 5.6.1 `ChaosClothConstraints.cpp:984`, the active edge-constraint constructor receives pattern data, but `PBDSpringConstraints.cpp:227` calls InitFromPatternData only when EdgeSpringWarpScale or EdgeSpringWeftScale properties exist. Legacy config conversion adds neither. The optional scaling also keeps 3D BaseDists and uses normalized UV directions. Do not rewrite pattern geometry on the earlier, incorrect assumption that projected lengths directly replace all edge rest lengths.

Added optional `-Iterations=1..16` to the read-only native motion probe. It overrides the component interactor, not saved assets, and reports the request alongside actual used iterations. Rebuilt successfully and ran `panel-iter.json` with eight iterations over the same nine frames. Every frame reports eight used iterations; independent fixed-particle validation passes. Worst edge ratio increases to 19.819. Smoothed editor simulation timing is about 4.95–5.29 ms versus 2.12–2.33 ms in the earlier one-iteration run. These sequential editor timings are not a controlled game FPS benchmark. Reject the higher count as a fix.

`panel-iter-protected.json` confirms unchanged asset hashes. Next isolate collision and self-collision contributions with transient controls, then inspect actual collider placement against the dressed body. A collider/motion change remains unproven. Preserve source geometry, body proportions and accepted animation; no game files or release archives were changed.
